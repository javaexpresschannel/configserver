server:
  port: 8068
spring:
  datasource:
    password: India@123
    url: jdbc:mysql://localhost:3306/stock-prod
    username: root
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
